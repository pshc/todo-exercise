# todilo

A basic Todo server+client app in React and Flask.

State is held in-memory in the server process, so no database is required.

## Setup

Requires node and Python 3.

```
npm install
pip3 install -r server/requirements.txt
```

## Run

```
npm start
```
